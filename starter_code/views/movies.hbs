<div class="row container-fluid">
        {{#each movies}}
    <div class="col-md-4 col-sm-12 movies-box">
        <h2>{{title}}</h2>
        <img src="{{image}}" alt="{{title}}">
        <a class="btn-seemore" href="/movies/{{_id}}">See more</a>
    </div>
        {{/each}}

</div>